# 交接文档(2026-07-06)

供新会话/新账号接续。完整逐日细节见 `.superpowers/sdd/progress.md`(git-ignored 台账,
Linux 机与 Mac 的 `~/Code/element-x-ios/.superpowers/sdd/` 各有一份副本)。

## 07-06 本地续会话更新(云端切本地后)

- 修完 handoff 里升级为必修的 bug:**space 邀请零露出**——根因是 `spaceFilterPublisher`
  只枚举已加入的道,邀请中的道从未进图谱、连 chip 都不出。方案 A(不单独出 chip,只在
  「全部」chip 用现成 `.overlayBadge` 挂红点):`HomeScreenModels.swift` 加
  `hasPendingSpaceInvites`,`HomeScreenViewModel.updateRooms()` 从
  `roomSummaryProvider` 里筛 `isSpace && joinRequestType?.isInvite && !seenInvites`
  算出这个值,`SpaceTabBarView`/`HomeScreenContent` 接线。
- 顺手发现并修了一个新 bug:**道条末尾配置(齿轮)按钮第一次点开正常,第二次起空白**。
  根因:`UserSessionFlowCoordinator.presentSpaceManagement()` 复用同一个
  `spacesSplitCoordinator` 实例,sheet 关闭时 `tearDown()` 把 sidebar/detail 协调器
  掏空,而一次性初始化守卫 `spacesFlowStarted` 从不重置,第二次直接把空壳 split 又
  present 一次。改法:照抄 `startSettingsFlow` 的范式,每次 present 都新建
  `NavigationSplitCoordinator` + `SpacesTabFlowCoordinator`,`setSheetCoordinator`
  传 `dismissalCallback` 释放引用。
- 两处改动均已 `xcodegen` + 全量 `xcodebuild build`(0 error)+ `swiftformat .`。**尚未
  真机验收**,并入下面移交事项 1。
- 产品判断确认:space 邀请概念该保留(上游原生功能,北极星文档已承诺保留 Space
  管理能力,多角色 homelab 下也是真实会发生的路径,非边缘场景)。
- 决策:打磨清单里「+号菜单加建道入口」「请旨横条深链到卡片」这两条**从零散打磨挪进
  Phase D**,与 D-4「需要你」聚合(邀请本就是其终局范围内的一类)一起设计交互,不再
  单独零敲碎打。
- 又修完 #7 打磨清单里的三项(均已 build 0 error + 相关单测跑绿,`swiftformat .` 过):
  - **`isPending` 同名不同义**:`AgentProjectSummary.swift` 的 `AgentChoiceStateIndexEvent.isPending`
    (拒绝清单:`status != "cancelled"`)与 `TimelineViewModel.swift` 私有 `isPending(_:)`
    (允许清单:`status == "pending"`)两套语义,`status` 为 `nil`/未来新状态值时会分歧。
    改法:删掉 `TimelineViewModel` 里的私有函数,两处调用点都改用
    `AgentChoiceStateIndexEvent.isPending` 这个唯一实现。`AgentProjectIndexServiceTests`
    里已有的 `choiceStateEventParsing*` 系列测试即是这条语义的回归覆盖,未新增测试。
  - **`rebuildIndex` cancel-previous**:`AgentTaskIndexService`/`AgentProjectIndexService`
    的 `rebuildIndex` 原来每次 debounce 触发都开一个不受追踪的 `Task`,先触发但跑得慢的
    可能用旧数据覆盖新数据。改法:两个 service 都加 `private var rebuildTask: Task<Void, Never>?`,
    每次 `rebuildIndex` 先 `cancel()` 上一个,任务体里每次进 for 循环、以及最终
    `.send()` 前都 `guard !Task.isCancelled else { return }`。**索引服务真正合并**(结构
    重复~40%)仍留 Phase D,这次只止血竞态,没做合并。
  - **PII 日志**:`AgentTasksScreenViewAction`/`AgentTaskPanelScreenViewAction` 补
    `CustomStringConvertible`(只打 case 名),堵住模板代码
    `MXLog.info("...received view action: \(viewAction)")` 把 `AgentTaskSummary.title`/
    `CanvasStep.label` 打进日志的口子。**范围只收窄在这两个 enum**,全仓 35 个屏幕同款
    模板代码的审计未做,视为独立事项。
  - 单测:`xcodebuild test -scheme UnitTests -destination "platform=iOS Simulator,id=0D2C8E22-14AF-48A3-A7CE-552709547033" -only-testing:UnitTests/AgentProjectIndexServiceTests -only-testing:UnitTests/AgentTaskIndexServiceTests -only-testing:UnitTests/TimelineViewModelTests`
    → 63/65 通过,失败的 2 个是已知存量的 `tapSendInfoEncryption*` 中文 locale 断言(非回归)。

## 当前状态:Phase C 全部完成

- 分支 `feature/space-tab-bar`,HEAD `97000ecdb`,Linux(/home/all/kaka/element-x-ios)与
  Mac(~/Code/element-x-ios)同步,远端 coderkaka(Mac 上叫 origin)。
- 已交付并全部通过 subagent 评审(每任务 spec+质量双验 + opus 终审):
  - **Phase A**:三种 agent 卡片(turn/choice_request/canvas.steps)+ state event 架构 + 差事 tab + SDK fork 修复
  - **Phase B**:房间任务面板(案卷)+ 进度 chip + thread(实录)绑定
  - **Phase C-1**:四 tab IA(政事/差事/书信/[检索关闭中])+ Spaces tab 移除(管理入齿轮)+ 书信 DM tab + 政事排除 DM
  - **Phase C-2**:io.element.agent.goal(SDK required_state + 解析 + AgentProjectIndexService)+ 案卡片增强(差事 x/y + 待批红标 + 排序)+ 请旨待批横条
  - **c2fix-1**:进房 chip 立即显示(state 枚举驱动,Task.id 改 taskID)
  - **c2fix-2**:政事堂标题 / 删空间按钮 / 删过滤按钮 / 道条常显+「全部」/ cancelled 语义 / 道选择持久化 / 道 chip 左移右移
  - **c2fix-3**:时间线差事卡片点击直达详情
- 设计锚点:`docs/superpowers/specs/2026-07-05-product-northstar-design.md`(御案体命名、
  四 tab、案卷/实录/附卷三分);Phase C spec 同目录;Phase D 只有 outlook 草案(故意的,
  等使用反馈再写 plan)。

## 环境与构建(关键事实)

- Linux 无 Swift 工具链。循环:Linux 改码 commit → push coderkaka → SSH Mac
  (`ssh -i ~/.ssh/id_ed25519_kaka zhangqiong@mac-mini.tail2edbaa.ts.net`)pull →
  `~/.local/bin/xcodegen`(新文件)/ `~/.local/bin/sourcery --config Tools/Sourcery/<配置>`
  (mock/preview)/ `/opt/homebrew/bin/swiftgen config run --config Tools/SwiftGen/swiftgen-config.yml`
  (字符串)→ `xcodebuild build … -destination "generic/platform=iOS"`(**零 `error:` 行
  即成功;CodeSign errSecInternalComponent 是 SSH keychain 限制,预期非错误**)→
  `xcodebuild test -scheme UnitTests -destination "platform=iOS Simulator,id=0D2C8E22-14AF-48A3-A7CE-552709547033" -only-testing:…`
  → Mac 侧生成物在 Mac 上 commit(SwiftFormat pre-commit 可能中止一次,重 add 重试)→ push → Linux pull。
- **测试要前台同步跑**(SSH 命令带大超时),后台分离进程会被资源竞争挤死(踩过)。
- 已知存量测试失败(非回归,勿修):2 个 tapSendInfoEncryption* 中文 locale 断言等约 13+1 skip。
- SDK fork:Mac `~/Code/matrix-rust-sdk-pinned`,分支 feature/agent-custom-events,
  HEAD `05ac042`(共 5 个 commit,无 GitHub 远端,备份 bundle 在
  `/Volumes/DeveloperData/backups/matrix-rust-sdk-agent-custom-events.bundle`)。
  重编:`CARGO_TARGET_DIR=/Volumes/DeveloperData/rust-target/matrix-rust-sdk-pinned cargo xtask swift build-framework --profile=reldbg`。
  **地雷**:xtask 构建尾段要求仓库根有 `target/` 目录(CARGO_TARGET_DIR 重定向后不存在,
  `git clean -dx` 会复发,mkdir 即修)。
- Matrix 侧:homeserver http://127.0.0.1:8008(Mac 本机),测试房间
  `!sYpMBnJhMtLtrRVuaY:…`(显示名 Hermes案)。token:hermes 在 `~/.hermes/.env`,
  agent-os 与各司在 `~/homelab/.secrets/matrix-agent-roles.env`,用户主账户(@qionghome)在
  `matrix-accounts.txt` 的 PRIMARY 项。**state key 里 event_id 的 $ 必须 %24 编码**。

## 用户设备验收清单(晚上真机测,汇总 c2fix/C-2 遗留 + 07-06 本地会话新改动)

**c2fix 系列 + C-2(此前已 code-side 完成,尚未真机走一遍)**
- [ ] 政事堂标题为静态文案(不再随选中道变化)
- [ ] 政事堂顶部已删除「空间」按钮 / 已删除「过滤」按钮
- [ ] 道条常显(不随滚动隐藏),且第一个是「全部」chip
- [ ] 请旨撤销(cancelled)不再被任何路径算作待批(横条 + 房间内面板都要看)
- [ ] 道选择跨启动持久化(选某道退出重进 app,还停在那道)
- [ ] 道 chip 长按可「左移」「右移」重新排序
- [ ] 进房间后差事 chip 立即显示(不用上滑刷新)
- [ ] 时间线里的差事卡片点击直达详情(不是只进房间)

**07-06 本地会话新改动(今天刚修,必须先验)**
- [ ] **space 邀请红点**:让另一个账号/角色新建一个道并邀请 @qionghome 加入,不接受,回到政事堂——
      「全部」chip 右上角应出现红点。接受或忽略邀请一段时间后(`seenInvites` 逻辑)红点应消失/不重复出现。
- [ ] **道条配置(齿轮)按钮**:点开管理面板 → 关闭 → **再点开第二次、第三次**,每次都要正常显示道列表,
      不能空白。这是本次的核心验收点,之前 100% 复现二次点击空白。
- [ ] `isPending`/`rebuildIndex`/PII 日志三项是内部逻辑修复,没有直接可点的 UI——**正常使用政事堂/差事面板
      时留意请旨待批的状态是否稳定一致**(横条说待批但面板说已解决,或反过来,就是回归了)即可,不用专门造场景。

1. **用户设备验收**(上面清单)。
2. **hermes B' 适配**(homelab 侧):协议 spec `~/homelab/docs/element-agent-protocol.md`
   版本 2026-07-05b + 07-06 增补(建道必须给君上 PL100 + agent-os PL50 / 过程消息必须进
   实录 thread / cancelled 已生效)。**该文档改动未 commit,等用户过目**。hermes 已开始
   写 ask-time pending(已实测)。
3. **磁盘清理**:DeveloperData 97% 满;方案=砍 x86_64 两架构缓存(省 46G)+ 以后构建
   `--only-target` 跳过 x86_64。**用户已朱批「晚点再说」,勿擅动**。
4. **快照基准录制**(用户 Xcode 跑 PreviewTests):新增/变更 preview 组一大批
   (案卡片/横条/道条/书信/AgentTasks navigationTitle/RoomListFilters 少一行)。
5. **Phase D**:草案 `specs/2026-07-05-phase-d-outlook-design.md`(看板/场景预设/归档/
   需要你聚合),等 C 用出反馈再写 spec/plan。
6. **SDK 上游 PR**:5 个 fork commit(changelog 占位 XXXX/XXXY/XXXZ/XXXW);先建 GitHub fork。
7. **defer 打磨清单**:~~space 邀请零露出~~、~~isPending 同名不同义~~、~~rebuildIndex
   cancel-previous~~、~~PII 日志(AgentTasksScreen/AgentTaskPanelScreen 两处)~~(以上均
   07-06 本地会话已修,见上,**均待真机/真实数据验收**)。剩:索引服务真正合并(结构
   重复~40%,留 Phase D 一起做)、内容检索改走 agent 能力、无主字符串/绑定残留清理
   (工具 `swift run tools unused-strings` 已就位,未跑过全仓扫描)、PII 日志模板审计
   (`MXLog.info("...received view action:")` 这行模板在全仓 35 个屏幕通用,只有本次
   涉及的 2 个 Agent 屏幕修了,其余屏幕是否也有 ViewAction 携带用户内容未审计)。
   (+ 号菜单建道入口、请旨横条深链已挪入 Phase D,见上)

## 工作方法(沿用)

subagent-driven-development:controller 写 brief(.superpowers/sdd/c*-brief.md)→ 派
实现者(sonnet)→ review-package 脚本出 diff → 派评审(sonnet;整 plan 终审用 opus)→
台账记账。产品向讨论直接与用户对话,拍板后进 brief。用户风格:决策快、真机验收勤、
中文交流;设计取舍给方案对比 + 明确推荐。
